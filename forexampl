print("Оленченко Максим Андрійович \n Лабораторна робота №4 \n Варіант 15"
      " Завдання №1 Написати функцію, яка з'ясовує, чи можна один з прямокутників цілком помістити в іншому.")
beep = True
while beep == True :
    len1 = (input("Введіть значення a довжини першого прямокутника: "))
    while(len1.isdigit() == False) : len1 = (input("Ви ввели не правильну форму заповнення. Введіть точніше : "))
    len1 = int(len1)
    len2 = (input("Введіть значення b довжини першого прямокутника: "))
    while(len2.isdigit() == False) : len2 = (input("Ви ввели не правильну форму заповнення. Введіть точніше : "))
    len2 = int(len2)
    len3 = (input("Введіть значення a довжини другого прямокутника: "))
    while(len3.isdigit() == False) : len3 = (input("Ви ввели не правильну форму заповнення. Введіть точніше : "))
    len3 = int(len3)
    len4 = (input("Введіть значення b довжини другого прямокутника: "))
    while(len4.isdigit() == False) : len4 = (input("Ви ввели не правильну форму заповнення.  Введіть точніше : "))
    len4 = int(len4)
    def res (len1, len2, len3, len4) :
        if (len1) == (len3) :
            if (len2) == (len4) :
                return ("Можна помістити перший прямокутник у другий, а другий - у перший")
            else :
                if (len2) > (len4) :
                    return ("Можна помістити другий прямокутник у перший")
                else :
                    return ("Можна помістити перший прямокутник у другий")
        elif (len1) > (len3) :
            if (len2) > (len4) :
                return ("Можна помістити другий прямокутник у перший")
            else :
                return ("Прямокутники не порівняні. ")
        elif (len1) < (len3) :
            if (len2) > (len4) :
                return ("Можна помістити перший прямокутник у другий")
            else :
                return ("Прямокутники не порівняні. ")
    m = res (len1, len2, len3, len4)
    print (m)
    ans = input("Чи бажаєте ви заново почати роботу з програмою? ")
    if ans == ("y") :
        beep = True
    else :
        beep = False
